
ADT Doubly Linked Chain:

createChain():
// Creates DLC with an start and endnode
// precondition:    self.startnode == None
                    self.endnode == None
                    self.size == None
// postcondition:   DLC has a size, startnode and endnode are defined

destroyChain()
// Destroys complete DLC
// precondition:
// postcondition:   self.startnode == None
                    self.endnode == None
                    self.size == None

isEmpty(): boolean
// Checks if DLC is empty
// precondition:
// postcondition:   return True: DLC is empty
                    return False: DLC is not empty

add(newnode: Node): boolean
// Adds newnode to DLC
// precondition:    next and previous node are legit
// postcondition:   return True: newnode added to DLC
                    return False: newnode not added to DLC

addLast(data: DLCItemType): boolean
// Adds newnode to DLC as last node
// precondition:
// postcondition:   return True: newnode added to DLC as last node
                    return False: newnode not added to DLC as last node

addFirst(data: DLCItemType): boolean
// Adds newnode to DLC as first node
// precondition:
// postcondition:   return True: newnode added to DLC as first node
                    return False: newnode not added to DLC as first node

remove(data: DLCItemType): boolean
// Removes node with data
// precondition:
// postcondition:   return True: node with data removed
                    return False: node with data not removed

removeLast(): boolean
// removes last node from DLC
// precondition:
// postcondition:   return True: last node removed
                    return False: last node not removed

removeFirst(): boolean
// removes first node from DLC
// precondition:
// postcondition:   return True: first node removed
                    return False: first node not removed

dataRetrieve(data: DLCItemType): Node
// Returns node with data if found
// precondition:
// postcondition:   return Node: node with data returned
                    return False: node with data not found




printChain():
// Prints the elements in the DLC
// precondition:
// postcondition: