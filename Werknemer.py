class Worker:

    def __init__(self, workerId, firstName, lastName, workLoad):
        self.workerId = workerId
        self.firstName = firstName
        self.lastName = lastName
        self.workLoad = workLoad
